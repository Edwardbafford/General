apiVersion: extensions/v1beta1
kind: Ingress
metadata:
  name: node-ingress
spec:
  rules:
  - host: node.example.com
    http:
      paths:
      - path: /
        backend:
          serviceName: node-app-loadbalancer
          servicePort: 80
